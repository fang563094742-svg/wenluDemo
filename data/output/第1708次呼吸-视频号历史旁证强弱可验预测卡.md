# 第1708次呼吸-视频号历史旁证强弱可验预测卡

## 当前前台唯一真值
- Safari 当前前台唯一真值仍是 `http://127.0.0.1:3210/`。

## 明确判断
- `https://channels.weixin.qq.com/platform/post/create` 比 `https://channels.weixin.qq.com/login.html` 更接近“较强可操作壳历史旁证”；但二者都只能算历史公开旁证，不能替代当前前台页。

## 3条关键依据
1. 当场直连 `create` 最终 URL 仍停在 `https://channels.weixin.qq.com/platform/post/create`，HTTP `200`。
2. 当场直连 `login` 最终 URL 仍停在 `https://channels.weixin.qq.com/login.html`，HTTP `200`。
3. 二者正文都命中“视频号助手”，但 `create` 的路径层级直接落在 `platform/post/create`，比 `login.html` 更接近可操作壳；这只是历史公开旁证强弱，不是当前执行页。

## 2个最可能出错点
1. 页面正文关键词区分度仍然偏弱，`create` 与 `login` 都可能返回相似壳页。
2. 我可能再次把“历史较强旁证”偷换成“当前执行页”。

## 主人回来即可快速确认的观察信号
- 触发条件：主人继续追问“视频号 create 和 login 哪个更强、当前页到底是什么”。
- 观察信号：我同主题回复第一句必须先锁定 `Safari -> http://127.0.0.1:3210/` 为当前前台唯一真值；随后才谈 `create > login` 的历史旁证强弱。
