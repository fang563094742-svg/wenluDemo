# 第1709次呼吸-历史旁证分层可验预测卡

## 明确判断
下一次同主题需要判当前页与历史旁证时，更稳的三层分界应是：当前前台唯一真值仍是 `Safari -> http://127.0.0.1:3210/`；微博 `newlogin` 与 `visitor` 都只能归为历史公开旁证中的登录壳，且二者正文都落到 `Sina Visitor System`；小红书 `publish/publish` 归为历史失败壳旁证。

## 3条关键依据
1. 用户当场直接给出的当前前台浏览器真值是 `Safari -> http://127.0.0.1:3210/`，不是微博或小红书页。
2. 当场直连 `https://weibo.com/newlogin?...` 的最终正文命中 `Sina Visitor System`，说明它会落到访客登录壳，不能冒充当前执行页。
3. 当场直连 `https://www.xiaohongshu.com/publish/publish` 返回的是小红书错误/404壳正文，仍应归到历史失败壳旁证。

## 2个最可能出错点
1. `newlogin` 与 `visitor` 的强弱排序也许仍需更细粒度区分，但它们都不应越级成当前前台页。
2. 若用户回来后前台真值已切换，这张卡里关于“当前前台唯一真值”的句子会立刻失效，需要按新真值重写。

## 主人回来可快速确认的观察信号
- 触发条件：用户下一次继续围绕“当前页 / 视频号 / 微博 / 小红书这些历史旁证怎么分层”追问。
- 快速确认信号：同主题回复第一句必须先锁定 `Safari -> http://127.0.0.1:3210/` 为当前前台唯一真值，再谈微博登录壳与小红书失败壳分层。
