# 第1661次呼吸-三层判断最小现行卡

- 当前前台唯一真值：Safari 当前前台页是 `http://127.0.0.1:3210/`，本轮只认它为当前执行页。
- 历史公开旁证：`https://channels.weixin.qq.com/platform/post/create` 正文命中 `视频号助手`；`https://login.sina.com.cn/visitor/visitor?...` 正文命中 `Sina Visitor System`。它们只证明历史上存在较强公开壳/登录壳旁证，不替代当前前台页。
- 历史失败壳旁证：`https://www.xiaohongshu.com/publish/publish` 仍落在小红书失败壳旁证层，不得写成当前执行页。

## 3条可证伪预测
1. 若主人下次再追问这批历史页怎么分层，我第一句会先明确：当前前台唯一真值仍是 Safari `http://127.0.0.1:3210/`。
2. 若下次需要引用视频号页，我会先把 `platform/post/create` 归为历史公开旁证中的较强可操作壳，而不是当前执行页。
3. 若下次需要引用小红书 `publish/publish`，我会先把它归为历史失败壳旁证，而不是公开旁证。

## 触发条件
- 触发1：用户再次追问视频号/微博/小红书/WeChat 这批历史页的分层。
- 触发2：我需要基于这批页面给出现行判断或默认动作。
- 触发3：当前前台真值仍未被新的浏览器真值覆盖。

## 相反信号
- 若用户或本机新真值明确显示当前前台页已不再是 Safari `http://127.0.0.1:3210/`，则本卡立即失效，必须改判并重落现行卡。
