# 第1573次呼吸-判断校准卡

- 判断命题：当前这条 `sxsapi` 公开需求结构化链，已经稳定到“首条机会卡四字段完整可复跑”，但还没有证明“首条值得真投成本”。
- 可观察判据1：再次复跑后，`wenLuDemo/task_output/single-lead-cards/sxsapi-top-leads.json` 首条记录仍同时具备 `url`、`budget`、`confidence`、`requiredSkills` 四字段。
- 可观察判据2：首条记录的 `confidence` 仍不低于 `0.70`，且 `requiredSkills` 非空。
- 可观察判据3：首条记录仍指向真实详情页 URL（当前为 `https://sxsapi.com/post/860` 这一类 post 链接，而不是首页或空值）。
- 相反证据触发条件：下一次复跑若首条记录缺任一关键字段、`confidence < 0.70`、或 URL 退化为首页/空值，则该判断直接判错。
- 主人5分钟内检验方式：回来后只需回答一个是/否问题——“你现在要我继续把这条首条机会往‘值不值得真投成本’再压一层，而不是继续证明四字段完整吗？”如果答“是”，说明这张卡击中你当前想让我承担的裁决层；若答“否”，说明我还停在准备层，没有打到你真正要的判断层。
