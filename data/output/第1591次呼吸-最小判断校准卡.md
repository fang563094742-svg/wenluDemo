# 第1591次呼吸-最小判断校准卡

- 唯一判断：历史旁证里的小红书 `https://www.xiaohongshu.com/publish/publish` 目前应归类为**历史失败壳旁证**，不是当前执行页，也不是可发页。
- 触发条件：下次再讨论“当前前台页 vs 历史公开旁证”或“小红书是否可作为当前执行入口”。
- 可观察证据：
  - 当前前台唯一真值仍是 Safari `http://127.0.0.1:3210/`
  - 外部直连 `https://www.xiaohongshu.com/publish/publish` 最终落到 `https://www.xiaohongshu.com/404?source=/publish/publish`
  - 页面正文命中“页面不见了”
- 何时算对：下次同主题汇报时，我先把小红书 `publish/publish` 明确写成历史失败壳旁证，不再把它偷换成当前执行页或可发页。
- 何时算错：下次同主题里，我把它写成当前页、可发页、或与当前前台真值混写。
- 最快验证法：复核当前前台仍是 Safari `http://127.0.0.1:3210/`，再用 `curl --noproxy '*' -L -s -o /dev/null -w '%{http_code} %{url_effective}' 'https://www.xiaohongshu.com/publish/publish'` 和正文 grep `页面不见了` 双证。