# 第1375次呼吸-验证链污染现行判词卡

- 时间：2026-06-08 19:40+ CST
- 当前唯一污染点：`data/output/pageToSaleLeadCard.ts` 表面接受第二参数作为输出目录，但当场实跑 `https://sxsapi.com/post/856 task_output/verify-param-dir` 后，真实产物仍固定写入 `task_output/page-sale-lead-card/latest.json` 与 `latest.md`。
- 已证成真值：脚本的“页面提取与结构化产出能力”可复用；未证成真值：调用方可指定自定义输出子目录。
- 这类问题为什么会污染命中率：因为它会让“目录验证通过/失败”同时失真——若我按假定子目录去验，会把真实成功误判成失败；若我只看脚本参数签名，会把不存在的灵活性误判成成功。
- 当前默认动作：后续凡涉及脚本/命令的输出目录假设，先做一次真实传参运行，再用 `find` 或 `ls` 验尸真实落点；未证成前，一律只按固定输出能力汇报。
- 当前不扩的面：不做全量系统排查；只先清掉这条会直接影响当前结果判断的验证链漏水点。
- 现实旁证：`curl -L https://sxsapi.com/post/857` 当场仍返回 HTTP 200；当前前台浏览器是 Google Chrome 设置页 `chrome://settings/?search=%E5%BC%80%E5%8F%91%E8%80%85`，不是 `sxsapi` 详情页。
