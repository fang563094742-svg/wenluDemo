# Chrome前台真值与历史旁证三层预测校准卡

生成时间：2026-06-09 08:43 CST

## 当前前台唯一真值
- 应用：Google Chrome
- 窗口/标签：`codex-chrome-js-ok`
- URL：`data:text/html,<title>codex-chrome-js-ok</title><h1>ok</h1>`
- 定性：这只是当前前台控制证明靶页，不是公开平台执行页。

## 历史公开旁证
- 较强可操作壳旁证：视频号助手 `https://channels.weixin.qq.com/platform/post/create`
- 登录壳旁证：微博 visitor `https://login.sina.com.cn/visitor/visitor`
- 其他公开旁证：微信 Web `https://web.wechat.com/`；即刻 `https://web.okjike.com/publish`

## 历史失败壳旁证
- 小红书 `https://www.xiaohongshu.com/publish/publish`
- 失败信号：历史标题已显示“你访问的页面不见了”，属于失败壳旁证，不能升级成当前执行页。

## 3条可证伪预测
1. 预测：如果没有新的前台切换证据，我下次同主题回复第一句会先锁定当前 Chrome `data:` 前台页，再谈历史旁证分层。
   - 概率：0.66
   - 触发条件：用户再次追问当前前台页与历史页分层。
   - 成功判据：我的回复第一句先点明 Chrome `data:` 页是当前前台唯一真值。
   - 失败判据：我先讲历史页、执行动作或旧 Safari 口径。

2. 预测：视频号 `platform/post/create` 仍比 `login.html` 更像较强可操作壳旁证，但不会替代当前 Chrome `data:` 前台页。
   - 概率：0.74
   - 触发条件：再次比较视频号各历史页层级。
   - 成功判据：分层里仍把 create 归为较强公开旁证，而不是当前页。
   - 失败判据：我把 create 偷换成当前执行页，或降到弱于 login.html。

3. 预测：小红书 `publish/publish` 在后续同主题判断里仍会被归为失败壳旁证，而非公开可操作页。
   - 概率：0.87
   - 触发条件：再次追问小红书页面层级。
   - 成功判据：继续明确它是失败壳旁证。
   - 失败判据：我把它写成公开可操作壳或当前页。

## 支持证据
- 用户刚给出的当前前台浏览器真值已明确为 Chrome `data:` 页。
- 最近浏览里视频号 create、微博 visitor、微信 Web、即刻 publish 都只作为历史页出现，而非当前页。
- 最近浏览里小红书 `publish/publish` 与 `404?source=/publish/publish` 均带失败标题或失败链路。

## 反证
- 当前前台页仍可能再次切换，旧预测会立刻失效。
- 历史公开页里某些入口即便可达，也可能只代表登录壳或历史可达，不能推出当前执行状态。

## 会推翻当前判断的观察信号
- 若用户或本机真值显示当前前台已切换到 Safari/Chrome 的非 `data:` 新页面，则这张卡的当前前台唯一真值立刻失效。

## 若判断错了怎么更新规则
- 立即结算相关预测。
- 把“同主题第一句先锁当前前台唯一真值”继续固化，但替换为新的当前前台页。
- 旧 `data:` 页降级为历史控制证明靶，不再当当前页引用。
