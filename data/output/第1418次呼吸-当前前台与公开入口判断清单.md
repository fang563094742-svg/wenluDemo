# 第1418次呼吸-当前前台与公开入口判断清单

时间：2026-06-08 22:21

## 已被当场验证的真值
- 当前前台浏览器：Google Chrome
- 当前前台标签：下载记录
- 当前前台 URL：`chrome://downloads/`
- 公开入口 `https://web.wechat.com/` 当场直连复核：HTTP 200
- 公开入口 `https://web.okjike.com/publish` 当场直连复核：HTTP 200

## 本轮判断清单（可直接结算）
1. 对象：`https://web.wechat.com/`
   - 情境：下一次再次用 `curl --noproxy '*' -L -s -o /tmp/... -w '%{http_code}'` 复核时
   - 预期结果：仍返回 `200`
   - 判定标准：输出为 `200` 则命中，否则落空
   - 置信度：0.78

2. 对象：`https://web.okjike.com/publish`
   - 情境：下一次再次用 `curl --noproxy '*' -L -s -o /tmp/... -w '%{http_code}'` 复核时
   - 预期结果：仍返回 `200`
   - 判定标准：输出为 `200` 则命中，否则落空
   - 置信度：0.77

3. 对象：当前前台浏览器真值
   - 情境：下一次再次读取 Google Chrome 前台活动标签时
   - 预期结果：仍为 `chrome://downloads/`，标题仍为“下载记录”
   - 判定标准：URL 与标题同时匹配则命中，否则落空
   - 置信度：0.72

## 最小可交付产物
- 本文件本身：把当前前台真值、两个公开入口真值、以及 3 条可结算预测压成单文件法源，用户回来后可直接核对真假。
