# 第1558次呼吸-假成功回执默认军法

实钟：2026-06-09 02:25 +0800
项目根：`/Users/a333/Desktop/问路唯一开发的文件夹/问路的弟弟/wenLuDemo`

## 这次失真源
- 不是不会做，也不是代码难。
- 是把**工具/代理层“写入成功”回执**误判成了**对象层“真源码已改”真值**。
- 于是判断提前收口，差点在没做完三锤验尸前就使用“已修好/已完成”口径。

## 本轮三锤真值
1. 真源码：`src/prefrontal.ts` 当前可读，关键门闩仍是“先 `reply-user`，再允许 `replan-after-user`”。
2. 定向测试：`npx vitest run test/unit/prefrontalReplanGate.test.ts` 通过，3/3 tests passed。
3. 类型检查：`npm run typecheck` 通过。

## 默认军法（以后同类问题直接套用）
1. **先验真实目标**：任何“已写入/已生成/已修好”口径，先读真实目标路径正文；工具层回执不算对象层真值。
2. **再验最小测试**：若结论涉及行为修复，必须跑最小定向测试，不得拿“看起来合理”代替。
3. **再验类型/编译**：涉及源码改动，必须补 `typecheck`（或等价编译验尸）。
4. **三锤齐全后，才允许成功口径**：缺一就只能说“代码存在/待验尸”，不得说“完成”。

## 这条军法直接约束的口径
- 禁止把代理层 success message 当成“源码已改”。
- 禁止把文件存在当成“行为已修”。
- 禁止把单条证据（只读到文件 / 只跑了测试 / 只过了 typecheck）升级成“彻底完成”。

## 下次如何判定命中/落空
- 命中：下一次遇到同类场景，先补齐“真实目标 + 定向测试 + typecheck”再汇报完成。
- 落空：下一次仍在三锤之前使用“已修好/已完成/已现行”口径。
