# Safari 3210 外部结构化证据卡

- 生成时间：2026-06-09T00:21:34Z
- 当前前台应用：`Safari`
- 最终 URL：`http://127.0.0.1:3210/`
- HTTP 状态：`200`
- 页面标题：`问路`
- 正文关键词：`问路`
- 历史旁证目录：`artifacts/public-demand-scan-1780943346983/scan.json`
- 历史公开最终 URL：`https://sxsapi.com/post/860`
- 历史正文关键词：`通过照片对比，识别冬虫夏草是人工的还是野生的`

## 分层说明

### 第 1 层：当前前台真值
当前前台 Safari 标签页真实停留在 `http://127.0.0.1:3210/`。这是通过 `osascript` 直接读取 Safari 当前 tab URL 与标题得到的实时真值，不依赖历史记忆。

### 第 2 层：HTTP 与页面正文真值
对 `http://127.0.0.1:3210/` 发起 HTTP 请求，返回 `200`，页面标题为 `问路`，正文命中关键词 `问路`。这证明 `3210` 不是空端口，而是当前可访问的问路页面。

### 第 3 层：历史公开旁证
历史公开扫描产物 `artifacts/public-demand-scan-1780943346983/scan.json` 中，首条最终线索落在 `https://sxsapi.com/post/860`，并保留正文摘录：`通过照片对比，识别冬虫夏草是人工的还是野生的，根据虫草形状和颜色区分产地`。这证明公开扫描链路曾落到正文级证据。

### 第 4 层：边界与结论
当前前台真值与历史公开旁证必须分层保存：前者证明“Safari 现在在哪”，后者证明“历史上公开链路抓到了什么”。不能把历史 `sxsapi` 正文页偷换成当前 Safari 前台页。

## 外部复核
- 复核脚本：`task_output/verify_safari_3210_external_evidence.sh`
- 通过标准：脚本退出码为 `0`，且同时满足当前前台 URL=`3210`、HTTP=`200`、页面关键词=`问路`、历史最终 URL=`https://sxsapi.com/post/860`、历史正文关键词命中。
