# 前台法源卡｜疯狂进化唯一现行军法

时间：2026-06-08 15:44 CST
目标：把“疯狂进化”压成唯一现行军法，直接改变下轮默认动作，禁止再以散卡复盘、子线先行或空转分析冒充进化。
完成事实：
- 当前默认进化总入口已被现行总卡收束为 `npm run tool:dominance`
- 真实 mind 账本中与“旧壳回滑 / 默认动作约束”直接相关的近期预测已结清；当前不存在同主题待结旧预测
- 认知升级线仍存在法源缺失，因此现行军法不能再把“继续扩卡/继续谈进化”当作默认动作，必须先服从唯一总控与断点处置

## 唯一现行军法
1. **先结算，再新增。**
   - 任何再谈“疯狂进化 / 默认动作 / 现行法源 / 旧壳回滑”前，先查 `mind.json` 里同主题旧预测是否已到可结状态。
   - 有可结旧预测：先结；未结清前，不得继续加新判断卡。
2. **先总控，再子线。**
   - “疯狂进化”的唯一现行入口不是继续翻旧卡，也不是直接跑某条子线；唯一默认入口是 `npm run tool:dominance`。
   - 只有总控失败且失败步被前台卡明确指出时，才允许切入对应子线处理。
3. **先断点，再分析。**
   - 当前唯一已知总断点是：`npm run cognitive:upgrade` 所依赖的 3 份法源缺失，导致认知升级线不是健康主入口。
   - 所以下轮若再碰“如何继续疯狂进化”，默认先报这个断点及其处置，不再先扩写“还能进化什么”。
4. **只产会改默认动作的卡。**
   - 新卡若不能直接改写下轮默认顺序（例如把动作收束成“先结算 / 先总控 / 先断点”），就不再生成。
   - 禁止同类主题重复新增“理解卡/判词卡/分析卡”充当进展。

## 下轮默认动作顺序
1. 先看本卡。
2. 先查同主题旧预测是否需要结算；若有，先 `settle_prediction`。
3. 再以 `data/output/唯一现行进化总卡.md` 为总导航，只认 `npm run tool:dominance` 为默认复跑入口。
4. 若总控失败，先读 `task_output/tool-dominance-line/latest-tool-dominance-frontdesk-card.md` 的失败步。
5. 若失败落在认知升级线，默认动作不是继续谈“疯狂进化”，而是优先补齐：
   - `问路判断骨架v2.md`
   - `问路判断骨架补强清单.md`
   - `问路现行判断卡｜《资治通鉴》× 帝王术骨架唯一现行入口.md`

## 前台唯一口径
- “疯狂进化”当前不是多入口探索题，而是单入口复跑题。
- 能改变现实的默认动作只有三件：结旧预测、跑总控、补认知升级线法源。
- 除这三件外，其余同主题扩写一律降级为非现行动作。

## 验证信号
- 下次再碰“疯狂进化/默认动作/法源”时，第一反应应是：先查旧预测、再看唯一总卡、再报认知升级线断点。
- 若下次仍先翻散卡、先聊子线、或先扩分析，则本卡判定失效。

## 一句话军令
**从现在起，“疯狂进化”的唯一现行军法就是：先结旧预测，后走 `npm run tool:dominance`，卡在认知升级线就先补法源；任何不能改变这三步默认顺序的动作，一律不算现行进化。**
