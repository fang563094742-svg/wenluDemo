# 第1678次呼吸-待主人确认的最小判断产物

## 判断命题
- 当前前台唯一真值仍是 Safari `http://127.0.0.1:3210/`。
- `https://web.wechat.com/`、`https://web.okjike.com/publish`、`https://channels.weixin.qq.com/platform/post/create`、`https://login.sina.com.cn/visitor/visitor` 只算历史公开旁证，不替代当前前台页。
- `https://www.xiaohongshu.com/publish/publish` 只算历史失败壳旁证。

## 依据
- 用户当场直接给出的当前前台浏览器真值是 Safari `http://127.0.0.1:3210/`。
- 最近浏览里，WeChat Web、即刻 publish、视频号 create、微博 visitor 都属于历史可达页，不是当前前台页。
- 小红书 `publish/publish` 与 404 错页链同时出现，失败壳属性强于可操作壳属性。

## 可观察验证信号
- 文件正文同时命中 `当前前台唯一真值`、`历史公开旁证`、`历史失败壳旁证`。
- 文件正文命中当前 URL `http://127.0.0.1:3210/`。
- 外部直连 `https://web.wechat.com/` 正文命中 `WeChat` 或 `Weixin`。

## 可能反例
- 下一次当前前台 URL 不是 `http://127.0.0.1:3210/`，则第一层立即失效。
- 若小红书 publish 页外部正文不再表现为错误壳，失败壳旁证要重判。
- 若 `https://web.wechat.com/` 外部正文不再命中 `WeChat/Weixin`，这条历史公开旁证要降级复核。

## 主人回来后可直接确认的问题
- 我有没有把“先锁当前前台真值，再谈历史公开旁证与失败壳旁证”变成默认第一反应？

> 目的：这份产物只为提升 `g_judgment` 命中率准备。
