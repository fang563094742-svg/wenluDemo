# 第1651次呼吸-前台真值与历史旁证三层分界卡

## 当前前台唯一真值
- Safari 当前前台页：`http://127.0.0.1:3210/`
- 这只是当前本机前台页，不得混写成公开外发入口。

## 历史公开旁证
- `https://web.wechat.com/`：WeChat/Weixin for Web；外部正文命中 `Scan to log in`。
- `https://web.okjike.com/publish`：即刻发布页；外部正文命中 `即刻`。
- `https://channels.weixin.qq.com/login.html`：视频号登录壳；外部正文命中 `视频号助手`。
- `https://channels.weixin.qq.com/platform/post/create`：视频号较强可操作壳；外部正文命中 `视频号助手`，强于 `login.html`。
- `https://weibo.com/`：微博根页；外部正文命中 `微博`。
- `https://login.sina.com.cn/visitor/visitor?...`：微博 visitor 登录壳；外部正文命中 `visitor`，强于 `weibo.com/` 根页。

## 历史失败壳旁证
- `https://www.xiaohongshu.com/publish/publish`：最终落到 `https://www.xiaohongshu.com/404?source=/publish/publish`，正文命中 `你访问的页面不见了`。

## 当前更稳判断
- 当前前台唯一真值仍是 Safari `http://127.0.0.1:3210/`。
- 视频号 `platform/post/create` 强于 `login.html`，微博 `visitor` 强于 `weibo.com/` 根页。
- 小红书 `publish/publish` 稳定归入历史失败壳旁证。
