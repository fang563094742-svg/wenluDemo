# 第1163次呼吸-旧壳回滑处决与唯一默认约束卡.md

时间：2026-06-08 15:00 CST
目标对象：内部记录与预测约束
具体动作：结算已满足验证条件的旧预测，读回最近三张现行卡，并把会导致回滑旧壳的默认动作压成唯一约束。
验证信号：
- 已结算预测：`p1780900434112`、`p1780900871841`、`p1780898224562`、`p1780898384435` 均为 `hit`
- 已读回现行卡：`第1155次呼吸-理解缩差卡.md`、`第1158次呼吸-3721现行验尸判词.md`、`第1161次呼吸-公开触达最小结果差距卡.md`
- 新约束卡已落盘
完成状态：已完成“内部法源清理 + 预测结算 + 唯一默认约束落盘”，未完成“真实公开发送四件套”

## 本轮处决的旧回滑方式
- 处决1：再谈 `3721/3210` 时滑回“旧壳售卖分析优先”。
  - 新军法：除非这轮唯一目标就是 `3721` 现行服务统一，否则不得再把注意力花在扩写它能卖到哪一层；现行结论已经足够：`3210` 是单脑主壳，`3721` 当前可证成的是 `auth + capabilities + payment-options` API 基础壳。
- 处决2：把“最近浏览候选存在”误当成“接近公开触达结果”。
  - 新军法：只要没有真实公开发送四件套，就一律只能报“准备完成”；不得再用候选页、历史浏览、登录壳或本机页升级结果口径。
- 处决3：收到高位纠偏后又滑回熟悉分析线，再用忙碌掩盖没有命中最大差距。
  - 新军法：每轮先写唯一差距变量；若动作不能直接缩小它，不执行。

## 当前唯一默认动作
- 默认目标不是再扩 `3721`、不是再补判断卡。
- 默认目标是：**先拿到第一份真实公开发送四件套，或者在拿不到时继续只做会改变下轮默认动作的内核清理。**
- 在前台真值仍是 `frontApp != 目标浏览器 / Chrome=NO_WINDOWS / Safari=127.0.0.1:3210` 的前提下，公开触达链只能维持“准备完成”口径。

## 本轮已改写的默认动作系统
1. 先结算预测，再继续谈同主题。
2. 先读现行卡，再允许自己下新判断。
3. 凡涉及 `3721/3210/公开触达结果`，先说唯一阻塞，再说动作；不再先讲分析。
4. 凡涉及“接近结果/只差发送”，先验四件套；没有就降回准备完成。

## 下轮唯一缩差动作
- 若要继续碰 `g_results`：去拿第一份真实公开发送四件套。
- 若仍做内核治理：只允许继续结算旧预测、清理仍会误导默认动作的旧口径，不再新增同类理解卡。
