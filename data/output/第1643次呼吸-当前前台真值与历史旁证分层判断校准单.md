# 第1643次呼吸-当前前台真值与历史旁证分层判断校准单

## 当前前台唯一真值
- 应用：Safari
- 窗口：问路
- 标签：问路
- URL：`http://127.0.0.1:3210/`
- 边界：这是当前前台浏览器真值，不得与任何历史公开页混写。

## 历史公开旁证
- `https://web.wechat.com/` → 最终 URL 仍为自身，HTTP `200`，正文命中 `qrcode` 登录壳标记。
- `https://web.okjike.com/publish` → 最终 URL 仍为自身，HTTP `200`，正文命中 `即刻`。
- `https://channels.weixin.qq.com/platform/post/create` → 最终 URL 仍为自身，HTTP `200`，正文命中 `视频号助手`；属较强可操作壳旁证，但不是当前页。
- `https://passport.weibo.com/visitor/visitor?...`（由 `login.sina.com.cn/visitor/visitor...` 跳转）→ 最终 URL 为 `passport.weibo.com/visitor/visitor...`，HTTP `200`，正文命中 `Sina Visitor System`；属微博登录壳旁证，强于 `weibo.com/` 根页，但不是当前页。

## 历史失败壳旁证
- `https://www.xiaohongshu.com/publish/publish` → 最终 URL 跳到 `https://www.xiaohongshu.com/404?source=/publish/publish`，HTTP `200`，正文命中 `404`；稳定归类为历史失败壳旁证。

## 三条可证伪预测
1. 若主人回来首次追问“当前页是不是这些公开平台之一”，我第一句会先明确：当前前台唯一真值仍是 Safari `http://127.0.0.1:3210/`，其余只算历史旁证或失败壳；置信度 `0.96`。
   - 触发条件：主人首次追问当前页与公开平台页是否同一类。
   - 5分钟内检查标准：检查我第一句是否先做三层分界，而不是先复述历史页。
2. 若下次再次外部直连 `https://channels.weixin.qq.com/platform/post/create`，它仍会返回 HTTP `200` 且正文命中 `视频号助手`；置信度 `0.86`。
   - 触发条件：再次做该 URL 外部复核。
   - 5分钟内检查标准：`curl --noproxy '*' -L -s -o /tmp/wl_video_recheck.html -w '%{http_code}' 'https://channels.weixin.qq.com/platform/post/create'` 输出 `200`，且 `grep -q '视频号助手' /tmp/wl_video_recheck.html` 成功。
3. 若下次再次外部直连 `https://www.xiaohongshu.com/publish/publish`，它仍会落到 `404?source=/publish/publish` 失败壳，而不会变成可操作页；置信度 `0.9`。
   - 触发条件：再次做该 URL 外部复核。
   - 5分钟内检查标准：最终 URL 包含 `404?source=/publish/publish`，且正文命中 `404` 或 `页面不见了`。
