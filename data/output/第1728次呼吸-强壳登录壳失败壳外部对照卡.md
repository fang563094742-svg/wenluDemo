# 第1728次呼吸-强壳登录壳失败壳外部对照卡

## 当前前台唯一真值
- Safari / `http://127.0.0.1:3210/`

## 历史公开旁证（较强可操作壳）
- 视频号 create：`https://channels.weixin.qq.com/platform/post/create`
- 最终 HTTP：`200`
- 正文关键词：`视频号助手`
- 归类：较强可操作壳旁证，不可偷换成当前前台页

## 历史公开旁证（登录壳）
- 微博 visitor：`https://login.sina.com.cn/visitor/visitor?...`
- 最终 HTTP：`200`
- 正文关键词：`Sina Visitor System`
- 归类：登录壳旁证，强于 `weibo.com/` 根页，但不可偷换成当前前台页

## 历史失败壳旁证
- 小红书 publish：`https://www.xiaohongshu.com/publish/publish`
- 最终 HTTP：`200`
- 正文关键词：`404`
- 归类：失败壳旁证，不可偷换成当前前台页或可操作壳

## 一个明确判断
- 判断：这组三类历史页里，`platform/post/create` 是较强可操作壳，`visitor` 是登录壳，`publish/publish` 是失败壳；但当前前台唯一真值仍只有 Safari `http://127.0.0.1:3210/`。

## 三条支撑依据
1. 当前前台浏览器真值明确指向 Safari `http://127.0.0.1:3210/`。
2. `platform/post/create` 外部直连最终 HTTP `200`，正文命中 `视频号助手`。
3. `visitor` 外部直连最终 HTTP `200`，正文命中 `Sina Visitor System`；`publish/publish` 外部直连正文命中 `404`。

## 两个可观测反例
1. 若下次当前前台浏览器真值不再是 Safari `http://127.0.0.1:3210/`，则该判断需重写当前前台层。
2. 若下次 `publish/publish` 正文不再命中 `404`，或 `create` / `visitor` 正文关键词消失，则三层强弱分层需重验。

## 最小验证动作
- 同时直连三页，抓最终 HTTP 与正文关键词；再核对本卡是否仍写着当前前台唯一真值 + 三层分界。

## 结果记录格式
- `front=<当前前台URL>; create=<HTTP>/<关键词>; visitor=<HTTP>/<关键词>; xhs=<HTTP>/<关键词>; verdict=<是否仍成立>`
