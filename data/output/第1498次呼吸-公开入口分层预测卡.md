# 第1498次呼吸-公开入口分层预测卡

## 明确结论
- 结论：在这组三条历史旁证公开页里，`https://channels.weixin.qq.com/platform/post/create` 比微博首页与小红书发布页更像可操作创建壳；微博更接近 `visitor/login` 壳，小红书发布页更接近 `404/错页` 壳。

## 3条支持信号
1. 视频号创建页直连最终返回 `HTTP 200`，正文命中 `视频号助手`。
2. 微博首页链路虽可达，但正文/链路命中 `visitor`/`login`，更像访客或登录壳。
3. 小红书 `publish/publish` 链路正文命中 `页面不见了` / `404`，更像错页壳而非可操作创建页。

## 2条反证
1. 若视频号创建页下一次复核正文只剩登录提示、且不再命中 `视频号助手` 或创建相关正文，则该判断降级。
2. 若微博或小红书对应页在下一次复核中出现明确创建/发布正文而不是 `visitor/login/404`，则该分层判断被推翻。

## 最小验证动作
- 动作：分别对三条 URL 做 `curl --noproxy '*' -L -s -D headers -o body`，同时检查 HTTP 链路与正文标记词。

## 结果回填模板
- 视频号创建页：HTTP=___；正文命中=___
- 微博：HTTP/链路=___；正文命中=___
- 小红书发布页：HTTP/链路=___；正文命中=___
- 最终判定：hit / miss

## 本轮现实证据
- 视频号创建页：`HTTP 200`；正文命中 `视频号助手`
- 微博：链路显示 `200/302`；正文命中 `Visitor/visitor`
- 小红书发布页：链路显示 `200/302`；正文命中 `页面不见了/404`
- 可验证任务：`vt1780936831298` 已通过
