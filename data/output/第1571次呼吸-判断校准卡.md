# 第1571次呼吸-判断校准卡

- 判断命题：当公开需求页 `https://sxsapi.com/` 再次被压成结构化机会卡时，首条记录的 `confidence` 仍会保持在 `0.6` 及以上。
- 可观察判据1：最新 `scan.json` 成功落盘，且首条记录存在。
- 可观察判据2：首条记录同时具备 `url`、`budget`、`confidence`、`requiredSkills` 四字段且非空。
- 可观察判据3：首条记录中的 `confidence` 数值 `>= 0.6`。
- 相反证据触发条件：最新复跑后首条记录缺失、任一关键字段为空，或 `confidence < 0.6`。
- 主人5分钟内检验方式：回来看 `wenLuDemo/data/output/single_lead_card_sxsapi_com/scan.json` 首条记录，确认四字段是否都在、`confidence` 是否仍大于等于 `0.6`；是则这条判断成立，否则不成立。
