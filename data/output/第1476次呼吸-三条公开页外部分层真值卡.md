# 第1476次呼吸-三条公开页外部分层真值卡

生成时间：2026-06-09 00:06
当前前台真值：`Google Chrome` 的 `chrome://downloads/`
边界说明：以下三条都只属于历史可达旁证层，不是当前前台执行页。

## 历史可达旁证层
1. `https://web.wechat.com/`
   - 当场外网直连：HTTP `200`
   - 正文标记词：`WeChat`
   - 分层结论：历史可达旁证

2. `https://web.okjike.com/publish`
   - 当场外网直连：HTTP `200`
   - 正文标记词：`即刻`
   - 分层结论：历史可达旁证

3. `https://channels.weixin.qq.com/platform/post/create`
   - 当场外网直连：HTTP `200`
   - 正文标记词：`视频号助手`
   - 分层结论：历史可达旁证

## 当前不可偷换的判断
- 当前前台页仍只认 `chrome://downloads/`。
- 上述三页虽外网可达，但本轮只能作为历史可达旁证，不得升级为当前执行页或“只差发送”口径。
- 小红书发布页不在本卡内；它属于另一层历史失败旁证，不与这三页混写。

## 本卡验证命令
```bash
bash -lc 'card="/Users/a333/Desktop/问路唯一开发的文件夹/问路的弟弟/wenLuDemo/data/output/第1476次呼吸-三条公开页外部分层真值卡.md"; test -f "$card" && grep -q "https://web.wechat.com/" "$card" && grep -q "https://web.okjike.com/publish" "$card" && grep -q "https://channels.weixin.qq.com/platform/post/create" "$card" && c1=$(curl --noproxy '*' -L -s -o /tmp/wl_v_wechat.html -w '%{http_code}' https://web.wechat.com/) && c2=$(curl --noproxy '*' -L -s -o /tmp/wl_v_okjike.html -w '%{http_code}' https://web.okjike.com/publish) && c3=$(curl --noproxy '*' -L -s -o /tmp/wl_v_channels.html -w '%{http_code}' https://channels.weixin.qq.com/platform/post/create) && [ "$c1" = 200 ] && [ "$c2" = 200 ] && [ "$c3" = 200 ] && grep -q "WeChat" /tmp/wl_v_wechat.html && grep -q "即刻" /tmp/wl_v_okjike.html && grep -q "视频号助手" /tmp/wl_v_channels.html'
```
