# 第1586次呼吸-最小判断校准卡

- 当前前台真值：Safari `http://127.0.0.1:3210/`。
- 历史公开旁证：`https://web.wechat.com/`、`https://web.okjike.com/publish`、`https://www.v2ex.com/go/jobs` 等只算历史可达层，不得偷换成当前执行页。
- 本轮新增外部硬证据：`https://www.v2ex.com/go/jobs` 直连验证通过，HTTP 200，且正文命中 `V2EX` 与 `jobs`。

## 只留一个最关键判断
- 预测结论：如果主人下一次继续追问“当前前台页 vs 历史公开旁证”同主题，我会先锁 `Safari http://127.0.0.1:3210/` 为唯一当前页，再把 `V2EX jobs` 明确列入历史公开旁证层，而不会把它偷换成当前执行页。
- 触发条件：主人下一次继续围绕当前前台页、最近浏览、公开入口可达性三者关系追问。
- 可观察证据：我回答的第一结论先点名当前前台 Safari 页；随后单列 `V2EX jobs` 为历史公开旁证，不把它写成“当前页”“只差发送页”或“前台执行页”。
- 何时算对：满足上面两点。
- 何时算错：若我先讲历史页、或把 `V2EX jobs`/WeChat/即刻任一历史页偷换成当前页。
- 最快验证法：主人回来第一次问到这个同主题时，直接看我回答首两句的分层顺序。

## 本轮外部可验证任务
- 已打穿：`vt1780945971591`
- 验证命令：
  - `bash -lc 'body=$(mktemp); code=$(curl --noproxy "*" -L -s -o "$body" -w "%{http_code}" https://www.v2ex.com/go/jobs); grep -q "V2EX" "$body" && grep -qi "jobs" "$body" && [ "$code" = "200" ]'`
