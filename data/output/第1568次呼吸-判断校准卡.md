# 第1568次呼吸-判断校准卡

## 明确判断命题
当一个公开需求单页抽取能力已经被同一命令二次复跑，并且首条记录稳定保留 `url`、`budget`、`confidence`、`requiredSkills` 四字段且 `confidence >= 0.8`，那么这条能力应被判定为“已进入稳定可复跑层”，下一轮不该再把精力投入到同类字段完整性复核，而该转去更高一层的判断题（如筛选质量、排序命中率、是否值得真投成本）。

## 3条可观察判据
1. 同一外部源再次实跑成功，且真实输出文件存在于项目根 `task_output/single-lead-cards/`。
2. 首条记录四字段仍完整：`url` 非空、`budget` 存在、`confidence >= 0.8`、`requiredSkills` 为非空数组。
3. 该闭环已经能被单条退出码命令客观验证通过，而不是靠人工读结果解释。

## 相反证据触发条件
只要下一次复跑出现以下任一情况，就推翻上面的判断：
- 输出文件路径漂移或未生成；
- 首条记录任一关键字段缺失/空值；
- `confidence < 0.8`；
- 退出码验证失败。

## 主人5分钟检验方式
主人回来后只需看两件事：
1. 可验证任务 `vt1780943881716` 是否 passed；
2. `wenLuDemo/task_output/single-lead-cards/sxsapi-top-leads.json` 首条记录是否仍显示四字段完整。
若两者都成立，则这张卡判定为“是”；否则为“否”。

## 当前默认动作变化
在新的反证出现前：
- 不再重复押“这四字段会不会还在”这种旧题；
- 下一枪默认改打更高一层：结构化结果的排序质量、机会筛选命中率、或‘值不值得真投成本’。
