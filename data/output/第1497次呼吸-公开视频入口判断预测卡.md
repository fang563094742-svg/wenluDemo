# 第1497次呼吸-公开视频入口判断预测卡

- 生成时间：2026-06-09 00:37 +0800
- 明确结论：在当前这组三方入口里，`https://channels.weixin.qq.com/platform/post/create` 比 `https://weibo.com/` 与 `https://www.xiaohongshu.com/publish/publish` 更像可操作创建壳，而不只是 visitor/404 登录壳。

## 支持该结论的3条可观察信号
- 信号1：视频号创建页最终 HTTP 状态为 `200`，且正文标记词命中连续的 `视频号` / `create` / `post` 语义，而不是 `visitor` 或 `404`。
- 信号2：微博主页最终链路虽到 `200`，但 `Location` 明确跳向 `passport.weibo.com/visitor/visitor...`，正文也以 `visitor` 为主。
- 信号3：小红书发布页最终链路虽回 `200`，但 `Location` 明确落到 `/404?source=/publish/publish`，正文命中 `404` / `页面不见了`。

## 会推翻该结论的2条反证
- 反证1：若视频号创建页下次直连复核出现明确登录壳跳转、正文主要命中 `login/登录`，且不再出现创建语义，则该判断失效。
- 反证2：若微博或小红书对应入口下次复核不再落 visitor/404，而直接出现稳定创建语义页面，则该判断失效。

## 最小验证动作
- 运行五条 `curl --noproxy '*' -L -s -D ... -o ...` 复核 `视频号/微博/小红书/微信网页/即刻` 的 HTTP 头链路与正文标记词，再对比 visitor/404/create 三类命中。

## 结果回填模板
- 结果：`hit / miss`
- 视频号：`HTTP ___`｜`正文关键词 ___`
- 微博：`HTTP ___`｜`Location ___`｜`正文关键词 ___`
- 小红书：`HTTP ___`｜`Location ___`｜`正文关键词 ___`
- 结论修正：`保留 / 降级 / 反转`
