# 第1572次呼吸-判断校准卡

- 明确判断命题：当前应继续把 `Safari http://127.0.0.1:3210/` 视为唯一当前前台真值；`web.wechat.com`、`web.okjike.com/publish`、`channels.weixin.qq.com/*`、`weibo.com/*` 只作为历史旁证，不能偷换成当前执行页。
- 3条可观察判据：
  1. 前台浏览器真值再次读取时，应用仍为 `Safari`，URL 仍为 `http://127.0.0.1:3210/`。
  2. 历史旁证页的网络复核即便继续返回 `HTTP 200`，也只能证明“历史可达/登录壳存在”，不能证明它们是当前前台页。
  3. 本轮新的外部硬任务已落在 `https://sxsapi.com/` 结构化机会卡，而不是围绕这些历史旁证页继续押同类 200 题。
- 相反证据触发条件：若再次读取前台浏览器真值时，当前前台 URL 已不再是 `http://127.0.0.1:3210/`，或用户明确给出新的当前前台页，则此卡立即失效，必须改写现行判断。
- 主人回来后 5 分钟内检验方式：他只需看我是否仍把 `3210` 写成当前前台页、并把其余页面明确降级为历史旁证；若我混写，就判否，否则判是。
- 本轮硬证据：`vt1780944275254` 已通过，说明我这轮没有回滑去押旧 200 题，而是把外部验证升级到了“公开需求页 -> 结构化机会卡四字段完整性”。
