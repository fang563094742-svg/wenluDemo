# 第1732次呼吸-当前前台与历史旁证可验证判断单

- 明确判断：当前前台唯一真值仍是 Safari `http://127.0.0.1:3210/`；`channels.weixin.qq.com/platform/post/create` 与 `login.sina.com.cn/visitor/visitor...` 只属于历史公开旁证；`xiaohongshu.com/publish/publish` 属于历史失败壳旁证。
- 3条支撑依据：
  1. 当前前台浏览器真值由用户当场给定：应用 Safari，URL `http://127.0.0.1:3210/`。
  2. 视频号 create 外部直连结果：HTTP 200，最终 URL 仍为 `https://channels.weixin.qq.com/platform/post/create`，正文命中 `视频号助手`，说明它可作为较强历史公开旁证壳。
  3. 微博 visitor 外部直连结果：HTTP 200，最终 URL 落在 `passport.weibo.com/visitor/visitor...`，正文命中 `visitor`；小红书 publish 外部直连结果：HTTP 200 但最终 URL 跳到 `https://www.xiaohongshu.com/404?source=/publish/publish`，正文命中 `你访问的页面不见了`，因此它是失败壳旁证而非可操作页。
- 2个可观测反例：
  1. 若下一次前台浏览器真值不再是 Safari `http://127.0.0.1:3210/`，当前判断立即失效，必须改判新的当前前台唯一真值。
  2. 若 `https://www.xiaohongshu.com/publish/publish` 后续不再跳 404 且正文不再命中 `你访问的页面不见了`，它就不能继续归类为失败壳旁证。
- 最小验证动作：重新抓取三条历史页的最终 URL + 正文关键词，并核对本卡中的三层分界仍成立。
- 结果记录格式：`时间 | 当前前台URL | create最终URL+关键词 | visitor最终URL+关键词 | xhs最终URL+关键词 | 判断(hit/miss)`
