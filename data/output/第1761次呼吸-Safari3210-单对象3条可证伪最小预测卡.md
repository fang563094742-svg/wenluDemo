# Safari 3210 真值｜单对象3条可证伪最小预测卡

生成时间：2026-06-09 08:25 CST
预测对象：当前前台 Safari `http://127.0.0.1:3210/` 这一真值在后续判断中的使用口径。

## 锚点真值
- frontApp=Safari
- frontTitle=问路
- frontUrl=http://127.0.0.1:3210/
- truthBoundary=本轮当前前台正文真值只认 Safari 当前标签标题与 URL，不外推为任何公开平台正文。

## 3条可证伪判断
1. **首句锁真值预测**
   - claim=只要下一次仍围绕“Safari 3210 真值/当前前台页”追问，我的第一句会先锁定“当前前台唯一真值是 Safari `http://127.0.0.1:3210/`”。
   - confidence=0.86
   - falsifiableBy=若第一句先讲历史公开页、执行动作、脚本过程或其他平台入口，则此预测落空。
   - checkMethod=直接查看我下一次同主题回复的第一句。

2. **旁证不越级预测**
   - claim=只要后续再次提到 `web.wechat.com`、`okjike publish`、视频号 create、微博或小红书 publish 壳，我会明确把它们降级为历史旁证，而不会当成当前正文页。
   - confidence=0.9
   - falsifiableBy=若我把任一历史入口/壳层直接说成“当前正在操作的正文页”，则此预测落空。
   - checkMethod=检查后续同主题答复是否出现“当前正文页”级别偷换。

3. **变更先结算预测**
   - claim=如果 Safari 当前前台 URL 不再是 `http://127.0.0.1:3210/`，我会先承认旧真值失效并切换到新前台真值，不继续沿用 3210 口径。
   - confidence=0.82
   - falsifiableBy=若前台已切页而我仍继续引用 `3210` 作为当前前台真值，则此预测落空。
   - checkMethod=前台切页后，查看我第一时间是否先结算旧真值再给出新真值。

## 最小外部验证闭环
- verifyNow=当前前台 Safari 标签标题仍为“问路”且 URL 仍为 `http://127.0.0.1:3210/`。
- verifyLater=新预测卡文件存在，且三条预测都明示了 `claim`、`falsifiableBy`、`checkMethod`。
