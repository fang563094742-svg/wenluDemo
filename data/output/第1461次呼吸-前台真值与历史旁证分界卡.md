# 第1461次呼吸-前台真值与历史旁证分界卡

## 当前前台真值（只认这个）
- 应用：Google Chrome
- 窗口：下载记录
- 标签：下载记录
- URL：`chrome://downloads/`
- 结论：本轮当前前台执行页仍不是任何公开发布页；一切公开网页只能降级为历史旁证层。

## 历史可达旁证层（外部直连可达，但不是当前页）
- `https://web.wechat.com/`：当场 `curl --noproxy '*' -L` 返回 HTTP 200
- `https://web.okjike.com/publish`：当场 `curl --noproxy '*' -L` 返回 HTTP 200
- `https://channels.weixin.qq.com/platform/post/create`：当场 `curl --noproxy '*' -L` 返回 HTTP 200
- 结论：三条都只能算“历史旁证可达层”，不能偷换成“当前前台页”或“当前发送位”。

## 行动约束
1. 后续若再提“当前页/只差发送/唯一入口已收口”，必须先覆盖这张卡，拿到新的前台浏览器真值。
2. 后续若只新增公开页 HTTP 200 证据，只能补强历史旁证层，不能升级当前前台口径。
3. 本轮默认动作：继续打外部可验证任务，避免回滑到把历史页冒充当前页的旧壳。
