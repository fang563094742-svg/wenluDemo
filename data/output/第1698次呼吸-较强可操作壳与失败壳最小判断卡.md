# 第1698次呼吸-较强可操作壳与失败壳最小判断卡

- 当前前台唯一真值：`Safari -> http://127.0.0.1:3210/`
- 历史公开旁证中的较强可操作壳：`https://channels.weixin.qq.com/platform/post/create`
- 历史失败壳旁证：`https://www.xiaohongshu.com/publish/publish`

## 最关键判断
`https://channels.weixin.qq.com/platform/post/create` 比 `https://www.xiaohongshu.com/publish/publish` 更适合作为历史公开旁证中的较强可操作壳；后者应稳定归类为失败壳，不得混写成可操作页。

### 3个客观依据
1. 外部正文级证据：`platform/post/create` 直连正文命中 `视频号助手`。
2. 外部正文级证据：`publish/publish` 直连正文命中 `404` 与 `访问的页面不见了`。
3. 当前前台真值仍是 `Safari -> http://127.0.0.1:3210/`，因此两者都只能做历史旁证，不能偷换成当前执行页。

### 可能打脸信号
如果后续当场抓取 `publish/publish` 正文不再是错误壳，而出现稳定可操作正文或登录后创建正文，则此判断需要降级重判。

## 备选判断
在历史公开旁证内部，`platform/post/create` 强于 `login.html` 这类登录壳，因为它更接近实际发帖/create 语义层；但它仍不是当前前台页。

### 3个客观依据
1. `platform/post/create` URL 语义直接指向 `post/create`。
2. 当场外部正文命中 `视频号助手`，说明不是空白连接失败壳。
3. 用户给出的当前前台真值已锁定为 Safari 本机页，因此该 create 页只能归为历史公开旁证层。

### 可能打脸信号
如果后续 `platform/post/create` 仅回登录壳、错误壳或不可达，而 `login.html` 反而能稳定进入更接近发帖的可操作流程，则该强弱排序需重判。
