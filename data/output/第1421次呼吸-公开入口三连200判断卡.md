# 第1421次呼吸-公开入口三连200判断卡

时间：2026-06-08 22:26 后本轮复核
当前前台浏览器真值：Google Chrome / 下载记录 / `chrome://downloads/`

## 当前可直接判真伪的3条判断
1. 对象：`https://web.wechat.com/`
   - 情境：使用 `curl --noproxy '*' -L` 直连复核
   - 预期结果：HTTP 200
   - 判定标准：命令输出状态码为 `200`
   - 置信度：0.86

2. 对象：`https://web.okjike.com/publish`
   - 情境：使用 `curl --noproxy '*' -L` 直连复核
   - 预期结果：HTTP 200
   - 判定标准：命令输出状态码为 `200`
   - 置信度：0.84

3. 对象：`https://channels.weixin.qq.com/platform/post/create`
   - 情境：使用 `curl --noproxy '*' -L` 直连复核
   - 预期结果：HTTP 200
   - 判定标准：命令输出状态码为 `200`
   - 置信度：0.87

## 最小交付物
- 本卡本身就是可读判断清单。
- 本轮当场复核结果：三条入口状态码均为 `200`。
- 历史旁证与当前前台严格分开：前台只认 `chrome://downloads/`，外网页仅作公开入口可达性旁证。
